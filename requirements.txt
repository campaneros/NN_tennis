# Streamlit Cloud / predict-only environment. Training (xgboost) and
# diretta.it scraping (playwright) stay local — see README_STREAMLIT.md.
--extra-index-url https://download.pytorch.org/whl/cpu
streamlit>=1.40
torch>=2.2,<3
numpy<2
pandas>=2.2,<3
scikit-learn==1.6.1
matplotlib>=3.8
certifi
